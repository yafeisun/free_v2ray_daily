name: Update Free Nodes

on:
  schedule:
    # 每天北京时间下午3点 (UTC时间7点) 运行
    - cron: '0 7 * * *'
  
  workflow_dispatch:  # 允许手动触发
    inputs:
      force_update:
        description: '强制更新所有节点'
        required: false
        default: 'false'
        type: boolean

jobs:
  update-nodes:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
        persist-credentials: true
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r config/requirements.txt
        pip install pysocks  # 支持 SOCKS5 代理
    
    - name: Install Cloudflare WARP
      run: |
        # 添加 Cloudflare GPG 密钥
        curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
        
        # 添加 Cloudflare 仓库
        echo 'deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ jammy main' | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
        
        # 更新包列表并安装 warp-cli
        sudo apt-get update
        sudo apt-get install -y cloudflare-warp
        
        # 清理并重新安装以确保干净状态
        echo "重新安装WARP以确保干净状态..."
        sudo apt-get purge -y cloudflare-warp || true
        sudo apt-get install -y cloudflare-warp || true
    
    - name: Connect to Cloudflare WARP
      run: |
        # 启动 WARP 服务
        sudo systemctl enable --now warp-svc

        # 等待服务完全初始化
        echo "等待WARP服务完全初始化..."
        for i in {1..30}; do
          if sudo systemctl is-active --quiet warp-svc; then
            echo "✓ WARP服务已启动 ($i/30)"
            break
          fi
          sleep 2
        done

        # 检查服务状态
        sudo systemctl status warp-svc --no-pager || true

        # 彻底重置WARP状态
        echo "彻底重置WARP状态..."
        sudo warp-cli --accept-tos disconnect || true
        sleep 3
        
        # 注册 WARP（使用匿名模式）
        sudo warp-cli --accept-tos registration new || true
        sleep 5

        # 连接 WARP
        sudo warp-cli --accept-tos connect || true

        # 等待连接完全建立
        echo "等待WARP连接完全建立..."
        for i in {1..60}; do
          STATUS=$(sudo warp-cli --accept-tos status 2>&1 | grep -o "Status update:.*" | head -1 || echo "")
          if echo "$STATUS" | grep -q "Success"; then
            echo "✓ WARP已连接成功 ($i/60)"
            break
          fi
          if [ $i -eq 60 ]; then
            echo "⚠️ WARP连接超时，继续执行..."
          fi
          sleep 1
        done

        # 验证最终状态
        echo "WARP最终状态："
        sudo warp-cli --accept-tos status || true

        # 检查SOCKS5代理端口（最多等待30秒）
        echo "检查SOCKS5代理端口40000（最多等待30秒）..."
        for i in {1..30}; do
          if sudo ss -tlnp | grep -q ":40000"; then
            echo "✓ SOCKS5代理端口40000已监听 ($i/30)"
            break
          fi
          if [ $i -eq 30 ]; then
            echo "⚠️ SOCKS5代理端口40000未监听，继续执行..."
          fi
          sleep 1
        done

        # 测试代理连接
        echo "测试代理连接..."
        for attempt in {1..3}; do
          if timeout 15 curl -x socks5://127.0.0.1:40000 -s -I https://www.google.com 2>&1 | grep -q "HTTP"; then
            echo "✓ 代理连接正常 (尝试 $attempt/3)"
            break
          else
            echo "✗ 代理连接失败 (尝试 $attempt/3)"
            if [ $attempt -lt 3 ]; then
              echo "等待5秒后重试..."
              sleep 5
            fi
          fi
        done

        # 强制重启warp-svc服务以确保代理端口正常工作
        echo "强制重启warp-svc服务以确保代理端口正常工作..."
        sudo systemctl restart warp-svc
        sleep 10

        # 验证重启后状态
        echo "验证重启后状态..."
        sudo warp-cli --accept-tos status || true
        for i in {1..20}; do
          if sudo ss -tlnp | grep -q ":40000"; then
            echo "✓ SOCKS5代理端口40000已监听（重启后）"
            break
          fi
          if [ $i -eq 20 ]; then
            echo "⚠️ 重启后仍未监听，继续执行..."
          fi
          sleep 1
        done

        echo "WARP代理已设置，SOCKS5 端口: 40000"
        echo "注意：如果WARP代理不可用，Python代码会自动降级为直连"
    
    - name: Run node collector
      id: collector
      env:
        ALL_PROXY: socks5://127.0.0.1:40000
        FORCE_UPDATE: ${{ github.event.inputs.force_update || 'false' }}
        GIT_EMAIL: ${{ secrets.GIT_EMAIL || 'action@github.com' }}
        GIT_NAME: ${{ secrets.GIT_NAME || 'GitHub Action' }}
        http_proxy: socks5://127.0.0.1:40000
        https_proxy: socks5://127.0.0.1:40000
      run: |
        echo "开始节点收集..."
        echo "代理设置："
        echo "  http_proxy=$http_proxy"
        echo "  https_proxy=$https_proxy"
        echo "  ALL_PROXY=$ALL_PROXY"
        python3 src/main.py
        echo "节点收集完成"
      continue-on-error: true
    
    - name: Check for collection changes
      id: collection_changes
      run: |
        echo "检查收集完成的文件状态..."
        echo "当前目录内容:"
        ls -la
        echo ""
        echo "result 目录内容:"
        ls -la result/ || echo "result 目录不存在"
        echo ""
        
        # 检查nodetotal.txt是否存在
        if [ -f result/nodetotal.txt ]; then
          echo "result/nodetotal.txt 存在"
          total_count=$(wc -l < result/nodetotal.txt)
          echo "总节点数: $total_count"
          
          echo ""
          echo "Git 状态:"
          git status
          
          echo ""
          echo "检查nodetotal.txt变更..."
          if git diff --quiet HEAD -- result/nodetotal.txt; then
            echo "has_nodetotal_changes=false" >> $GITHUB_OUTPUT
            echo "没有检测到收集更新"
          else
            echo "has_nodetotal_changes=true" >> $GITHUB_OUTPUT
            echo "total_node_count=$total_count" >> $GITHUB_OUTPUT
            echo "检测到收集更新，需要提交nodetotal.txt"
          fi
        else
          echo "result/nodetotal.txt 不存在"
          echo "has_nodetotal_changes=false" >> $GITHUB_OUTPUT
        fi
    
    - name: Commit nodetotal.txt (collection complete)
      if: steps.collection_changes.outputs.has_nodetotal_changes == 'true'
      run: |
        git config --global user.email "${{ secrets.GIT_EMAIL || 'action@github.com' }}"
        git config --global user.name "${{ secrets.GIT_NAME || 'GitHub Action' }}"
        
        # 只提交nodetotal.txt
        git add result/nodetotal.txt
        
        total_count="${{ steps.collection_changes.outputs.total_node_count }}"
        commit_msg="Collect nodes - $(date '+%Y-%m-%d %H:%M:%S') ($total_count total nodes)"
        
        git commit -m "$commit_msg"
        git push
        
        echo "收集节点已提交并推送到仓库"
    
    - name: Create summary (collection)
      if: always()
      run: |
        echo "## 📦 节点收集摘要" >> $GITHUB_STEP_SUMMARY
        echo "- **运行时间**: $(date '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
        echo "- **触发方式**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
        
        if [ "${{ steps.collection_changes.outputs.has_nodetotal_changes }}" == "true" ]; then
          echo "- **更新状态**: ✅ 成功更新" >> $GITHUB_STEPMARY
          echo "- **总节点数**: ${{ steps.collection_changes.outputs.total_node_count }}" >> $GITHUB_STEP_SUMMARY
        else
          echo "- **更新状态**: ℹ️ 无更新" >> $GITHUB_STEPMARY
        fi
        
        if [ "${{ steps.collector.outcome }}" != "success" ]; then
          echo "- **收集状态**: ⚠️ 收集过程中出现错误" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [ "${{ steps.collector.outcome }}" != "success" ]; then
          echo "- **收集状态**: ⚠️ 收集过程中出现错误" >> $GITHUB_STEP_SUMMARY
          echo "- **后续动作**: ❌ 不会触发测速" >> $GITHUB_STEP_SUMMARY
        fi
          if [ -f result/nodetotal.txt ]; then
            total_count=$(wc -l < result/nodetotal.txt)
            echo "- **总节点数**: $total_count" >> $GITHUB_STEP_SUMMARY
          fi
        else
          echo "- **更新状态**: ℹ️ 无更新" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [ "${{ steps.collector.outcome }}" != "success" ]; then
          echo "- **收集状态**: ⚠️ 收集过程中出现错误" >> $GITHUB_STEP_SUMMARY
        fi
    
    - name: Notify on failure
      if: failure()
      run: |
        echo "❌ 节点更新失败"
        echo "请检查日志以获取详细信息"
        exit 1