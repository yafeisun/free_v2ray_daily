# 🏗️ 项目重复架构问题分析

## 🔍 当前重复问题

### 1. 收集器重复 ❌
```
scripts/collectors/          ← 运行时副本 (13个文件)
├── cfmem.py                  ← 与src版本不同
├── clashgithub.py            ← 与src版本不同
└── ...

src/collectors/              ← 源码版本 (15个文件)  
├── cfmem.py                  ← 与scripts版本不同
├── clashgithub.py            ← 与scripts版本不同
├── base_collector.py         ← 核心基类，scripts中没有
├── example_site.py           ← 示例，scripts中没有
└── ...
```

### 2. SubsCheck重复 ❌
```
subscheck/                    ← 运行时工具 (81MB)
├── bin/subs-check            ← 81MB二进制文件
├── config/config.yaml        ← 配置文件
└── config/speedtest_config.yaml

src/subscheck/                ← Python代码模块
├── config.py                 ← 配置模块
├── manager.py                ← 管理器模块
└── __init__.py
```

## 🎯 合理的架构设计

### 方案A: src为主，scripts为入口 ✅
```
src/                          ← 核心源码
├── collectors/               ← 唯一收集器源码
├── subscheck/                ← 唯一subscheck模块
├── testers/                  ← 测试器模块
├── utils/                    ← 工具模块
└── ...

scripts/                      ← 运行时入口
├── run_collectors.py         ← 调用src/collectors
├── run_speedtest.py          ← 调用src/testers
├── run_subscheck.py          ← 调用src/subscheck
└── ...                       ← 简单的调用脚本

subscheck/                    ← 二进制工具 (保留)
├── bin/subs-check            ← 二进制文件
└── config/                   ← 配置文件
```

### 方案B: 按功能完全分类 ✅
```
src/                          ← 核心代码
├── node_collectors/          ← 节点收集
├── speed_testers/            ← 节点测速
├── subscheck_integration/   ← subscheck集成
└── ...

tools/                        ← 外部工具
├── subscheck/                ← 二进制工具
└── ...

scripts/                      ← 简化脚本 (仅保留main.py等)
├── main.py                   ← 主入口
└── utils/                    ← 简单工具
```

## 🔄 解决方案

### 推荐方案：统一架构，消除重复

1. **删除重复的收集器**
   - 保留 `src/collectors/` 作为源码
   - 删除 `scripts/collectors/`
   - 创建统一的收集器调用接口

2. **明确SubsCheck职责**
   - `src/subscheck/` → Python封装模块
   - `subscheck/` → 二进制工具和配置
   - 两者职责不同，保留两者

3. **简化scripts目录**
   - 保留主要的运行脚本
   - 移除重复的功能实现
   - 专注于调用src中的模块

## 📋 清理步骤

1. 删除 `scripts/collectors/` 重复文件
2. 创建 `scripts/run_collectors.py` 调用src
3. 更新导入路径和调用方式
4. 保留 `subscheck/` 作为工具目录
5. 保留 `src/subscheck/` 作为Python模块

## 🎯 目标架构

```
项目根目录/
├── src/                       ← 🔥 唯一源码目录
│   ├── collectors/           ← 节点收集器
│   ├── subscheck/            ← subscheck Python模块
│   ├── testers/              ← 测试器
│   └── utils/                ← 工具模块
├── scripts/                   ← 🔥 运行时入口
│   ├── main.py               ← 主入口
│   ├── run_collectors.py     ← 收集器调用
│   ├── run_speedtest.py      ← 测速调用
│   └── utils/                ← 简单工具
├── subscheck/                 ← 🔥 外部工具
│   └── bin/subs-check        ← 81MB二进制
└── config/                    ← 🔥 配置文件
```

这样既消除了重复，又保持了清晰的职责分离！