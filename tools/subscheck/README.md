# SubsCheck 测速工具

## 📁 目录结构

```
tools/subscheck/
├── bin/                    # subs-check 二进制文件
│   ├── subs-check         # 主程序
│   └── subs-check.tar.gz  # 安装包
├── config/                # 配置文件
├── speedtest/             # Python测速脚本
│   ├── test_nodes_with_subscheck.py    # 主要测速脚本
│   ├── intelligent_timeout.py          # 智能超时管理
│   ├── test_nodes_batch.py             # 批量测试
│   └── ...                            # 其他测试脚本
├── config.py             # Python配置接口
├── manager.py            # 测速管理器
└── __init__.py           # 包初始化
```

## 🚀 使用方法

### 安装subs-check

```bash
# 下载并安装subs-check
python3 tools/subscheck/bin/download_subscheck.py
```

### 运行测速

```bash
# 使用Python脚本进行测速
python3 tools/subscheck/speedtest/test_nodes_with_subscheck.py
```

## 📋 功能特性

- **智能超时管理**：根据网络状况动态调整超时时间
- **批量测试**：支持大量节点的并发测试
- **性能监控**：实时监控测试进度和性能指标
- **结果分析**：生成详细的测试报告

## ⚙️ 配置说明

配置位于 `config.py` 文件中，可以自定义：
- 测试超时时间
- 并发连接数
- 测速服务器地址
- 输出格式设置